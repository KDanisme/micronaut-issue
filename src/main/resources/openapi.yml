openapi: 3.1.0
info:
  version: "1"
  title: api
paths:
  /example-route:
    get:
      responses:
        "200":
          content:
            "application/json":
              schema:
                type: array
                uniqueItems: true
                items:
                  type: string
